[package]
name = "kairos-cli"
description = "Fleet tool for the Kairos umbrella folder: status, compile-gate, scaffold, sibling patches, hardening tables, the C oracle, and deploy each package as its own GitHub repository."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true

[[bin]]
name = "kairos"
path = "src/main.rs"

[dependencies]
kairos-alloc = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
rusty_symbols = { workspace = true }

[lints]
workspace = true
