[package]
name = "list-cost"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
rusty_rtos_core = { path = "../../../rusty_rtos_core/crates/rusty_rtos_core", default-features = false }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
